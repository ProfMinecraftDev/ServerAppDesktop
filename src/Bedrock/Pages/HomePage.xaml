<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="ServerAppDesktop.Bedrock.Pages.HomePage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:ServerAppDesktop.Bedrock.Pages"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <ScrollViewer>
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
            </Grid.RowDefinitions>
            <StackPanel Orientation="Horizontal" Spacing="8" Grid.Row="0" 
                VerticalAlignment="Top" Padding="16,12">
                <FontIcon Glyph="&#xE80F;" FontWeight="SemiBold" FontSize="28"/>
                <TextBlock Text="Inicio" TextWrapping="Wrap" Style="{ThemeResource TitleTextBlockStyle}" />
            </StackPanel>
            <Grid RowSpacing="20" ColumnSpacing="20" Grid.Row="1" Padding="24,10">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                </Grid.RowDefinitions>

                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="*" />
                </Grid.ColumnDefinitions>

                <Border Background="{ThemeResource CardBackgroundFillColorDefault}" Padding="16" Grid.Row="0" Grid.Column="0" CornerRadius="{ThemeResource ControlCornerRadius}">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="Auto" />
                        </Grid.ColumnDefinitions>

                        <StackPanel Orientation="Horizontal" Spacing="8" Grid.Column="0">
                            <FontIcon Glyph="&#xE7E8;" Foreground="{ThemeResource AccentFillColorDefaultBrush}" FontSize="20" FontWeight="SemiBold" />
                            <TextBlock TextWrapping="Wrap" Text="Estado del servidor" Style="{ThemeResource BodyStrongTextBlockStyle}" />
                        </StackPanel>

                        <TextBlock x:Name="ServerStatusText" Style="{ThemeResource CaptionTextBlockStyle}" TextWrapping="Wrap" Text="Detenido" Grid.Column="1" />
                    </Grid>
                </Border>

                <Border Grid.Row="0" Grid.Column="1" Background="{ThemeResource CardBackgroundFillColorDefault}" Padding="16" CornerRadius="{ThemeResource ControlCornerRadius}">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="Auto" />
                        </Grid.ColumnDefinitions>

                        <StackPanel Orientation="Horizontal" Spacing="8" Grid.Column="0">
                            <FontIcon Glyph="&#xE74C;" Foreground="{ThemeResource AccentFillColorDefaultBrush}" FontSize="20" FontWeight="SemiBold" />
                            <TextBlock TextWrapping="Wrap"
                                       Text="Edición" Style="{ThemeResource BodyStrongTextBlockStyle}" />
                        </StackPanel>

                        <TextBlock x:Name="ServerTypeText" TextWrapping="Wrap" Style="{ThemeResource CaptionTextBlockStyle}" Text="Bedrock" Grid.Column="1" />
                    </Grid>
                </Border>

                <Border Grid.Row="1" Grid.Column="0" Background="{ThemeResource CardBackgroundFillColorDefault}" Padding="16" CornerRadius="{ThemeResource ControlCornerRadius}">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="Auto" />
                        </Grid.ColumnDefinitions>

                        <StackPanel Orientation="Horizontal" Spacing="8" Grid.Column="0">
                            <FontIcon Glyph="&#xE8F7;" Foreground="{ThemeResource AccentFillColorDefaultBrush}" FontSize="20" FontWeight="SemiBold" />
                            <TextBlock TextWrapping="Wrap"
                                       Text="Ruta de Servidor" Style="{ThemeResource BodyStrongTextBlockStyle}" />
                        </StackPanel>

                        <TextBlock x:Name="ServerPathText" TextWrapping="Wrap" Style="{ThemeResource CaptionTextBlockStyle}" Text="No disponible :(" Grid.Column="1" />
                    </Grid>
                </Border>
                
                <Border Grid.Row="1" Grid.Column="1" Background="{ThemeResource CardBackgroundFillColorDefault}" Padding="16" CornerRadius="{ThemeResource ControlCornerRadius}">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="Auto" />
                        </Grid.ColumnDefinitions>

                        <StackPanel Orientation="Horizontal" Spacing="8" Grid.Column="0">
                            <FontIcon Glyph="&#xE943;" Foreground="{ThemeResource AccentFillColorDefaultBrush}" FontSize="20" FontWeight="SemiBold" />
                            <TextBlock TextWrapping="Wrap"
                                       Text="Ejecutable" Style="{ThemeResource BodyStrongTextBlockStyle}" />
                        </StackPanel>

                        <TextBlock x:Name="ExecutablePathText" TextWrapping="Wrap" Style="{ThemeResource CaptionTextBlockStyle}" Text="No hay :(" Grid.Column="1" />
                    </Grid>
                </Border>

                <Border Grid.Row="2" Grid.Column="0" Background="{ThemeResource CardBackgroundFillColorDefault}" Padding="16" CornerRadius="{ThemeResource ControlCornerRadius}">
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="*" />
                            <RowDefinition Height="Auto" />
                        </Grid.RowDefinitions>

                        <FontIcon Glyph="&#xEC27;" Foreground="{ThemeResource AccentFillColorDefaultBrush}" HorizontalAlignment="Left" FontSize="28" Grid.Row="0" />
                        <StackPanel Spacing="10" Grid.Row="1" Margin="0,50,0,0">
                            <TextBlock Text="IP de Servidor" Style="{ThemeResource SubtitleTextBlockStyle}" />
                            <TextBlock x:Name="ServerIpText" Text="XXX.XXX.XXX.X" Style="{ThemeResource BodyStrongTextBlockStyle}" />
                        </StackPanel>
                    </Grid>
                </Border>
                
                <Border Grid.Row="2" Grid.Column="1" Background="{ThemeResource CardBackgroundFillColorDefault}" Padding="16" CornerRadius="{ThemeResource ControlCornerRadius}">
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="*" />
                            <RowDefinition Height="Auto" />
                        </Grid.RowDefinitions>

                        <FontIcon Glyph="&#xE839;" Foreground="{ThemeResource AccentFillColorDefaultBrush}" HorizontalAlignment="Left" FontSize="28" Grid.Row="0" />
                        <StackPanel Spacing="10" Grid.Row="1" Margin="0,50,0,0">
                            <TextBlock Text="Puerto de Servidor" Style="{ThemeResource SubtitleTextBlockStyle}" />
                            <TextBlock x:Name="ServerPortText" Text="XXXXX" Style="{ThemeResource BodyStrongTextBlockStyle}" />
                        </StackPanel>
                    </Grid>
                </Border>
                
                <!-- Botones de control del servidor -->
                <StackPanel Grid.Row="3" Grid.ColumnSpan="2" Orientation="Horizontal" 
                           HorizontalAlignment="Center" Spacing="12" Margin="0,20,0,0">
                    <Button x:Name="StartServerButton" Style="{ThemeResource AccentButtonStyle}" MinWidth="120">
                        <StackPanel Orientation="Horizontal" Spacing="8">
                            <FontIcon Glyph="&#xE768;" />
                            <TextBlock Text="Iniciar" />
                        </StackPanel>
                    </Button>
                    <Button x:Name="StopServerButton" MinWidth="120">
                        <StackPanel Orientation="Horizontal" Spacing="8">
                            <FontIcon Glyph="&#xE71A;" />
                            <TextBlock Text="Detener" />
                        </StackPanel>
                    </Button>
                    <Button x:Name="RestartServerButton" MinWidth="120">
                        <StackPanel Orientation="Horizontal" Spacing="8">
                            <FontIcon Glyph="&#xE72C;" />
                            <TextBlock Text="Reiniciar" />
                        </StackPanel>
                    </Button>
                </StackPanel>
            </Grid>
        </Grid>
    </ScrollViewer>
</Page>
