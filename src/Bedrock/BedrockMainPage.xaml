<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="ServerAppDesktop.Bedrock.BedrockMainPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:ServerAppDesktop.Bedrock"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <!-- ✨ Transiciones suaves de página porque somos elegantes -->
    <Page.Transitions>
        <TransitionCollection>
            <ReorderThemeTransition />
        </TransitionCollection>
    </Page.Transitions>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*" /> <!-- Contenido principal -->
        </Grid.RowDefinitions>

        <!-- 🧭 Navegación: El GPS de nuestra app -->
        <NavigationView IsSettingsVisible="True" SelectionChanged="NavigationView_SelectionChanged"
                        IsBackButtonVisible="Collapsed" Grid.Row="0"
                        ItemInvoked="NavigationView_ItemInvoked">
            <NavigationView.MenuItems>
                <NavigationViewItem Icon="Home" Content="Inicio" Tag="HomePage" IsSelected="True" />
                <NavigationViewItem Content="Archivos" Tag="FilesPage" >
                    <NavigationViewItem.Icon>
                        <FontIcon Glyph="&#xEC50;" />
                    </NavigationViewItem.Icon>
                </NavigationViewItem>
            </NavigationView.MenuItems>
            <NavigationView.FooterMenuItems>
                <NavigationViewItem Content="Acerca de" Icon="Help" Tag="AboutPage" />
            </NavigationView.FooterMenuItems>
            <Frame x:Name="NavigateFrame" />
        </NavigationView>
    </Grid>
</Page>
