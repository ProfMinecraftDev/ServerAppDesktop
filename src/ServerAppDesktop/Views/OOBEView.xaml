<?xml version="1.0" encoding="utf-8" ?>
<Page
    x:Class="ServerAppDesktop.Views.OOBEView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:animatedvisuals="using:Microsoft.UI.Xaml.Controls.AnimatedVisuals"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="using:ServerAppDesktop.Views"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:toolkit="using:CommunityToolkit.WinUI.Controls"
    xmlns:toolkitExtensions="using:CommunityToolkit.WinUI"
    mc:Ignorable="d">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <TextBlock
            x:Uid="OOBEView_WelcomeText"
            Grid.Row="0"
            MaxWidth="1064"
            Margin="0,40,0,36"
            Style="{StaticResource TitleTextBlockStyle}"
            TextAlignment="Center"
            TextWrapping="Wrap" />

        <ScrollViewer
            Grid.Row="1"
            Padding="36,0,36,0"
            VerticalScrollBarVisibility="Auto"
            VerticalScrollMode="Auto">
            <Border>
                <StackPanel Spacing="{StaticResource SettingsCardSpacing}">
                    <StackPanel.ChildrenTransitions>
                        <TransitionCollection>
                            <RepositionThemeTransition />
                        </TransitionCollection>
                    </StackPanel.ChildrenTransitions>

                    <TextBlock x:Uid="AppearanceHeader" Style="{StaticResource SettingsSectionHeaderTextBlockStyle}" />

                    <toolkit:SettingsCard x:Uid="BackdropSettingsCard" HeaderIcon="{toolkitExtensions:FontIcon Glyph=&#xF156;}">
                        <ComboBox Width="170" SelectedIndex="0">
                            <ComboBoxItem x:Uid="MicaBackdropItem" />
                            <ComboBoxItem x:Uid="MicaAltBackdropItem" />
                            <ComboBoxItem x:Uid="DesktopAcrylicBackdropItem" />
                        </ComboBox>
                    </toolkit:SettingsCard>

                    <toolkit:SettingsCard x:Uid="ThemeSettingsCard" HeaderIcon="{toolkitExtensions:FontIcon Glyph=&#xE790;}">
                        <ComboBox Width="150" SelectedIndex="0">
                            <ComboBoxItem x:Uid="SystemThemeItem" />
                            <ComboBoxItem x:Uid="LightThemeItem" />
                            <ComboBoxItem x:Uid="DarkThemeItem" />
                        </ComboBox>
                    </toolkit:SettingsCard>

                    <TextBlock x:Uid="ServerHeader" Style="{StaticResource SettingsSectionHeaderTextBlockStyle}" />

                    <toolkit:SettingsCard x:Uid="EditionSettingsCard" HeaderIcon="{toolkitExtensions:FontIcon Glyph=&#xE7FC;}">
                        <ComboBox Width="200" SelectedIndex="0">
                            <ComboBoxItem x:Uid="BedrockEditionItem" />
                            <ComboBoxItem x:Uid="JavaEditionItem" />
                        </ComboBox>
                    </toolkit:SettingsCard>

                    <toolkit:SettingsCard x:Uid="PathSettingsCard" HeaderIcon="{toolkitExtensions:FontIcon Glyph=&#xEDA2;}">
                        <StackPanel Orientation="Horizontal" Spacing="8">
                            <TextBlock x:Uid="NoPathSelectedText" VerticalAlignment="Center" />
                            <Button
                                x:Uid="BrowsePathButton"
                                VerticalAlignment="Center"
                                Style="{StaticResource AccentButtonStyle}">
                                <StackPanel Orientation="Horizontal" Spacing="8">
                                    <FontIcon Glyph="&#xE8DA;" />
                                    <TextBlock x:Uid="BrowsePathButtonText" />
                                </StackPanel>
                            </Button>
                        </StackPanel>
                    </toolkit:SettingsCard>

                    <toolkit:SettingsCard x:Uid="ExecutableSettingsCard" HeaderIcon="{toolkitExtensions:FontIcon Glyph=&#xE943;}">
                        <StackPanel Orientation="Horizontal" Spacing="8">
                            <TextBlock x:Uid="NoFileSelectedText" VerticalAlignment="Center" />
                            <Button
                                x:Uid="BrowseExeButton"
                                VerticalAlignment="Center"
                                Style="{StaticResource AccentButtonStyle}">
                                <StackPanel Orientation="Horizontal" Spacing="8">
                                    <FontIcon Glyph="&#xE8E5;" />
                                    <TextBlock x:Uid="BrowseExeButtonText" />
                                </StackPanel>
                            </Button>
                        </StackPanel>
                    </toolkit:SettingsCard>

                    <toolkit:SettingsCard x:Uid="PortSettingsCard" HeaderIcon="{toolkitExtensions:FontIcon Glyph=&#xE839;}">
                        <StackPanel Orientation="Horizontal" Spacing="8">
                            <CheckBox x:Uid="UsePropertiesCheckBox" />
                            <NumberBox
                                x:Uid="ServerPortNumberBox"
                                Width="200"
                                LargeChange="100"
                                Maximum="65535"
                                Minimum="1"
                                SmallChange="1" />
                        </StackPanel>
                    </toolkit:SettingsCard>

                    <toolkit:SettingsCard x:Uid="DescriptionSettingsCard" HeaderIcon="{toolkitExtensions:FontIcon Glyph=&#xE932;}">
                        <TextBox x:Uid="ServerDescriptionTextBox" MinWidth="100" />
                    </toolkit:SettingsCard>

                    <TextBlock x:Uid="StartupHeader" Style="{StaticResource SettingsSectionHeaderTextBlockStyle}" />

                    <toolkit:SettingsCard x:Uid="AutoStartSettingsCard" HeaderIcon="{toolkitExtensions:FontIcon Glyph=&#xE78B;}">
                        <StackPanel Orientation="Horizontal" Spacing="8">
                            <InfoBadge Style="{StaticResource CautionIconInfoBadgeStyle}" />
                            <ToggleSwitch x:Uid="AutoStartToggleSwitch" />
                        </StackPanel>
                    </toolkit:SettingsCard>
                </StackPanel>
            </Border>
        </ScrollViewer>

        <InfoBar
            x:Uid="AttentionInfoBar"
            Grid.Row="2"
            Margin="36,24,36,36"
            IsClosable="False"
            IsOpen="True">
            <InfoBar.ActionButton>
                <Button x:Uid="GoButton" />
            </InfoBar.ActionButton>
        </InfoBar>
    </Grid>
</Page>
