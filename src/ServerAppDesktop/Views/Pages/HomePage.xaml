<?xml version="1.0" encoding="utf-8" ?>
<Page
	x:Class="ServerAppDesktop.Views.Pages.HomePage"
	xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
	xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
	xmlns:controls="using:ServerAppDesktop.Controls"
	xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
	xmlns:local="using:ServerAppDesktop.Views.Pages"
	xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
	xmlns:toolkitExtensions="using:CommunityToolkit.WinUI"
	NavigationCacheMode="Required"
	xmlns:toolkitUIControls="using:CommunityToolkit.WinUI.UI.Controls"
	mc:Ignorable="d">

	<Grid>
		<Grid.RowDefinitions>
			<RowDefinition Height="Auto" />
			<RowDefinition Height="*" />
		</Grid.RowDefinitions>

		<TextBlock
			x:Uid="Page_Home_Title"
			Grid.Row="0"
			Margin="36,40,36,0"
			Style="{StaticResource PageTitleTextBlockStyle}" />

		<Grid Grid.Row="1" Padding="36">
			<Grid.RowDefinitions>
				<RowDefinition Height="Auto" />
				<RowDefinition Height="*" />
			</Grid.RowDefinitions>

			<StackPanel
				Grid.Row="0"
				HorizontalAlignment="Center"
				Orientation="Horizontal"
				Spacing="12">
				<Button x:Uid="StartServerButton">
					<StackPanel Orientation="Horizontal" Spacing="8">
						<SymbolIcon Symbol="Play" />
						<TextBlock x:Uid="StartServerText" />
					</StackPanel>
				</Button>
				<Button x:Uid="StopServerButton">
					<StackPanel Orientation="Horizontal" Spacing="8">
						<SymbolIcon Symbol="Stop" />
						<TextBlock x:Uid="StopServerText" />
					</StackPanel>
				</Button>
				<Button x:Uid="RestartServerButton">
					<StackPanel Orientation="Horizontal" Spacing="8">
						<SymbolIcon Symbol="Refresh" />
						<TextBlock x:Uid="RestartServerText" />
					</StackPanel>
				</Button>
			</StackPanel>

			<ScrollViewer
				Grid.Row="1"
				MaxWidth="1200"
				Margin="0,36,0,0">

				<Grid
					ChildrenTransitions="{StaticResource DefaultLayoutTransitions}"
					ColumnSpacing="16"
					RowSpacing="16">

					<Grid.ColumnDefinitions>
						<ColumnDefinition Width="*" />
						<ColumnDefinition Width="*" />
					</Grid.ColumnDefinitions>

					<Grid.RowDefinitions>
						<RowDefinition Height="Auto" />
						<RowDefinition Height="Auto" />
						<RowDefinition Height="Auto" />
						<RowDefinition Height="Auto" />
					</Grid.RowDefinitions>

					<controls:InfoCard
						x:Uid="ServerEditionCard"
						Grid.Row="0"
						Grid.Column="0"
						Icon="{toolkitExtensions:FontIcon Glyph=&#xE7FC;}" />

					<controls:InfoCard
						x:Uid="ServerVersionCard"
						Grid.Row="0"
						Grid.Column="1"
						Icon="{toolkitExtensions:FontIcon Glyph=&#xE74C;}" />

					<controls:InfoCard
						x:Uid="ServerPathCard"
						Grid.Row="1"
						Grid.Column="0"
						Icon="{toolkitExtensions:SymbolIcon Symbol=SyncFolder}" />

					<controls:InfoCard
						x:Uid="ServerExecutableCard"
						Grid.Row="1"
						Grid.Column="1"
						Icon="{toolkitExtensions:FontIcon Glyph=&#xE943;}" />

					<controls:SquareCard
						x:Uid="ServerIPCard"
						Grid.Row="2"
						Grid.Column="0"
						Icon="{toolkitExtensions:FontIcon Glyph=&#xE88A;}" />

					<controls:SquareCard
						x:Uid="ServerPortCard"
						Grid.Row="2"
						Grid.Column="1"
						Icon="{toolkitExtensions:FontIcon Glyph=&#xEC27;}" />

					<controls:InfoCard
						x:Uid="ActiveWorldCard"
						Grid.Row="3"
						Grid.ColumnSpan="2"
						Icon="{toolkitExtensions:FontIcon Glyph=&#xE909;}" />
				</Grid>
			</ScrollViewer>
		</Grid>
	</Grid>
</Page>