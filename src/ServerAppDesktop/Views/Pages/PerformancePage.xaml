<?xml version="1.0" encoding="utf-8" ?>
<Page
	x:Class="ServerAppDesktop.Views.Pages.PerformancePage"
	xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
	xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
	xmlns:controls="using:ServerAppDesktop.Controls"
	xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
	xmlns:local="using:ServerAppDesktop.Views.Pages"
	xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
	xmlns:toolkitExtensions="using:CommunityToolkit.WinUI"
	NavigationCacheMode="Required"
	mc:Ignorable="d">


	<Grid>
		<Grid.RowDefinitions>
			<RowDefinition Height="Auto" />
			<RowDefinition Height="*" />
		</Grid.RowDefinitions>

		<TextBlock
			x:Uid="Page_Performance_Title"
			Grid.Row="0"
			Margin="36,40,36,0"
			Style="{StaticResource PageTitleTextBlockStyle}" />

		<ScrollViewer
			Grid.Row="1"
			MaxWidth="1200"
			Margin="36">

			<Grid
				ChildrenTransitions="{StaticResource DefaultLayoutTransitions}"
				ColumnSpacing="16"
				RowSpacing="16">

				<Grid.ColumnDefinitions>
					<ColumnDefinition Width="*" />
					<ColumnDefinition Width="*" />
				</Grid.ColumnDefinitions>

				<Grid.RowDefinitions>
					<RowDefinition Height="Auto" />
					<RowDefinition Height="Auto" />
					<RowDefinition Height="Auto" />
				</Grid.RowDefinitions>

				<controls:InfoCard
					x:Uid="CPUCard"
					Grid.Row="0"
					Grid.Column="0"
					Icon="{toolkitExtensions:FontIcon Glyph=&#xE950;}" />

				<controls:InfoCard
					x:Uid="RAMCard"
					Grid.Row="0"
					Grid.Column="1"
					Icon="{toolkitExtensions:FontIcon Glyph=&#xE964;}" />

				<controls:InfoCard
					x:Uid="NetworkCard"
					Grid.Row="1"
					Grid.Column="0"
					Icon="{toolkitExtensions:FontIcon Glyph=&#xE701;}" />

				<controls:InfoCard
					x:Uid="DiskCard"
					Grid.Row="1"
					Grid.Column="1"
					Icon="{toolkitExtensions:FontIcon Glyph=&#xE958;}" />

				<controls:SquareCard
					x:Uid="PlayersCard"
					Grid.Row="2"
					Grid.ColumnSpan="2"
					Icon="{toolkitExtensions:FontIcon Glyph=&#xE902;}" />
			</Grid>
		</ScrollViewer>
	</Grid>
</Page>
